//! This crate intentionally ships no code. It exists only to host the
//! process-level end-to-end tests under `tests/`, which spawn the real built
//! binaries together. Cargo requires a package to declare a target, so this
//! empty library is the minimal stand-in.
