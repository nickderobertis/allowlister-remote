# Normalize line endings to LF on checkout across platforms so Windows CI
# (biome fmt-check) sees the same bytes as Linux/macOS. All tracked text is
# already LF; this only changes working-tree checkout behavior, not content.
* text=auto eol=lf

# Binary assets must never be line-ending normalized.
*.png binary
*.woff2 binary
